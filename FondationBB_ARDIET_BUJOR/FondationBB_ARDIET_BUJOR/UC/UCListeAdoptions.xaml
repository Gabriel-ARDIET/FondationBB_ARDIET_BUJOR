<UserControl x:Class="FondationBB_ARDIET_BUJOR.Windows.UCListeAdoptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="1280" Background="#FFFFFF">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Nom client" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtFiltreNom" Height="30" Margin="0,0,0,5" TextChanged="FiltreAdoption_Changed"/>

                    <TextBlock Text="Prénom client" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtFiltrePrenom" Height="30" Margin="0,0,0,5" TextChanged="FiltreAdoption_Changed"/>

                    <TextBlock Text="Nom animal" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtFiltreAnimal" Height="30" TextChanged="FiltreAdoption_Changed"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgAdoptions" AutoGenerateColumns="False" IsReadOnly="True" 
                          VerticalScrollBarVisibility="Auto" ItemsSource="{Binding}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nom" Binding="{Binding Adoptant.Nom, ValidatesOnExceptions=True}" Width="*"/>
                        <DataGridTextColumn Header="Prénom" Binding="{Binding Adoptant.Prenom, ValidatesOnExceptions=True}" Width="*"/>
                        <DataGridTextColumn Header="Nom animal adopté" Binding="{Binding UnAnimal.Nom, ValidatesOnExceptions=True}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <StackPanel Grid.Column="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                    <Button x:Name="btnAjouter" ToolTip="Ajouter" FontFamily="Segoe MDL2 Assets" Content="&#xE109;" Width="40" Height="40" Margin="0,0,5,0" Click="BtnAjouter_Click"/>
                    <Button x:Name="btnEditer" ToolTip="Éditer" FontFamily="Segoe MDL2 Assets" Content="&#xE104;" Width="40" Height="40" Margin="0,0,5,0" Click="BtnEditer_Click"/>
                    <Button x:Name="btnSupprimer" ToolTip="Supprimer" FontFamily="Segoe MDL2 Assets" Content="&#xE74D;" Width="40" Height="40" Click="BtnSupprimer_Click"/>
                </StackPanel>

                <Border Background="#F9F9F9" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20">
                    <StackPanel DataContext="{Binding SelectedItem, ElementName=dgAdoptions}">
                        <TextBlock Text="Contrat d'adoption" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                        <TextBlock Text="Nom"/>
                        <TextBox Text="{Binding Adoptant.Nom, Mode=OneWay}" Margin="0,0,0,10"/>

                        <TextBlock Text="Prénom"/>
                        <TextBox Text="{Binding Adoptant.Prenom, Mode=OneWay}" Margin="0,0,0,10"/>

                        <TextBlock Text="Date d'adoption"/>
                        <TextBox Text="{Binding DateAdoption, StringFormat=\{0:dd/MM/yyyy\}}" Margin="0,0,0,10"/>

                        <TextBlock Text="Montant des frais versés"/>
                        <TextBox Text="{Binding Frais}" Margin="0,0,0,10"/>

                        <Button Content="Télécharger le contrat ⬇" Padding="10" Click="BtnTelechargerContrat_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>