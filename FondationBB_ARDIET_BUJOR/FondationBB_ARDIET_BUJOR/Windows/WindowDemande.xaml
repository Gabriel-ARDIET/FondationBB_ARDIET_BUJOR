<Window x:Class="FondationBB_ARDIET_BUJOR.Windows.WindowDemande"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FondationBB_ARDIET_BUJOR.Windows"
        mc:Ignorable="d"
        Title="Enregistrement d'une Demande d'Adoption" Height="330" Width="500"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <ControlTemplate x:Key="ValidationTemplate">
            <DockPanel>
                <Border BorderBrush="#EF5350" BorderThickness="1.5" CornerRadius="3">
                    <AdornedElementPlaceholder x:Name="ControlCherche"/>
                </Border>
            </DockPanel>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTemplate}"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTemplate}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Top">

            <Label Content="Client Demandeur *"/>
            <Grid Margin="0,2,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="textClient" Grid.Column="0" Height="25" IsReadOnly="True" Background="#F5F5F5" VerticalContentAlignment="Center"
         Text="{Binding IdPersonne, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}"/>
                <Button x:Name="btnChoisirClient" Content="Choisir..." Grid.Column="1" Width="80" Margin="10,0,0,0" Height="25" Click="btnChoisirClient_Click"/>
            </Grid>

            <Label Content="Race souhaitée *"/>
            <ComboBox x:Name="comboRace" Margin="0,2,0,15" Height="25" VerticalContentAlignment="Center"
                      DisplayMemberPath="Libelle" 
                      SelectedValuePath="Id"
                      SelectedValue="{Binding IdRace, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Tranche d'âge de l'animal *"/>
            <ComboBox x:Name="comboTrancheAge" Margin="0,2,0,15" Height="25" VerticalContentAlignment="Center"
                      SelectedValue="{Binding UneTrancheAge, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnValider" Content="Valider" Width="120" Height="30" Margin="0,0,20,0" IsDefault="True" Click="btnValider_Click"/>
            <Button x:Name="btnAnnuler" Content="Annuler" Width="120" Height="30" IsCancel="True" Click="btnAnnuler_Click"/>
        </StackPanel>
    </Grid>
</Window>